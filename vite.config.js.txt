import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

// FIX: Ensures compatibility with Node >= 20
export default defineConfig({
  plugins: [react()],
  optimizeDeps: {
    esbuildOptions: {
      target: "esnext",
    },
  },
  server: {
    proxy: {
      "/api": "http://localhost:4000",
    }
  }
});
